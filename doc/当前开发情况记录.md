# 当前开发情况记录

## 2026-06-16 16:00

### Phase 3 短期记忆 + Checkpoint — 进行中

#### 已完成
- [x] `graph/main_graph.py` 引入 `SqliteSaver` 持久化 checkpoint，支持多轮对话
- [x] `main.py` 传入 `config={"configurable": {"thread_id": "..."}}` 激活 checkpoint
- [x] `main.py` `initial_state` 精简为只传 `messages`，checkpoint 自动恢复其他字段
- [x] `main.py` 支持 `reset` 命令（更换 thread_id 开始新会话）
- [x] `graph/main_graph.py` 添加 `should_retry` 条件路由：用户说"不满意/换一种"时回退到 DecomposeAgent 重新拆解
- [x] `llm_factory.py` 新增 `_llm_cache` 字典缓存 LLM 实例，避免每轮创建新实例
- [x] `SummaryAgent` 改用 `self.llm = create_llm(temperature=0.7)` 缓存实例
- [x] `IntentAgent` 新增 `_compress_history_if_needed` 上下文压缩：消息超过 20 条时 LLM 摘要旧对话，替换为一条 system 消息 + 保留最近 20 条
- [x] `IntentAgent` 使用 `IntentSchema` 结构化输出，修复 objective 输出分类标签的问题
- [x] `DecomposeAgent` 使用 `PlanSchema` 结构化输出 + `knowledge_context` 注入
- [x] 修复 `SqliteSaver.from_conn_string()` 在 3.x 变为上下文管理器的问题，改用 `SqliteSaver(sqlite3.connect(...))`

#### 已知问题
- [ ] `_compress_history_if_needed` 中 `state["messages"]` 赋值语法有误（`[dict] + recent` 应为列表拼接），需修复
- [ ] `graph/main_graph.py:53` 变量名拼写错误 `conten` → `content`，`should_retry` 函数无法正常运行
- [ ] `main.py:52` 仍有拼写错误 `knowledege_context` → `knowledge_context`

#### 待开发
- Phase 4: 长期记忆系统（热-温-冷三层）
- Phase 5: 反思机制 + 多 Agent 协作
- Phase 6: 反馈学习 + 生产保障

---

### Phase 2: RAG 知识库接入 — 已完成

- [x] `rag/document_loader.py` — 支持 PDF/Word/Markdown 三种格式，输出统一 Markdown
- [x] `rag/text_splitter.py` — `SmartTextSplitter` 按 Markdown 标题层级递归分块
- [x] `rag/retrieve.py` — `KnowledgeRetriever` 封装 ChromaDB 向量化存储 + 语义搜索
- [x] `scripts/load_documents.py` — 一键入库脚本
- [x] `data/knowledge_base/` — 存放 3 份 AI 学习文档（第一章/第二章/第三章）
- [x] `state/schemas.py` — Pydantic 结构化输出（IntentSchema, PlanSchema, SearchQuery）

#### 已知问题
- [x] `Qwen3-Embedding-0.6B` 语义区分度不足，检索结果经常不相关 — 待升级到 `text-embedding-v3`
- [x] `chunk_size=10` 在 Gitee embedding API 上必须设置，否则 400 错误

---

### Phase 1: 核心骨架 — 已完成

- [x] 配置加载 + LLM 连通
- [x] AgentState + Task 数据结构
- [x] IntentAgent / DecomposeAgent / SummaryAgent
- [x] 主图编排（intent → retrieve → decompose → summary）
- [x] 命令行交互入口
