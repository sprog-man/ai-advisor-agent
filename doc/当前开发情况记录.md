# 一、Phase1 启动前阶段

现在我们处在 **Phase 1 启动前**，需要先把地基代码写好。按顺序做：

1. **创建完整目录结构**（上面的目录树，空文件也行）
2. **编写 `config/settings.py`**（Pydantic版配置中心）
3. **编写 `core/llm_factory.py`**（LLM工厂，封装兼容逻辑）
4. **编写 `state/agent_state.py`**（数据结构定义）
5. **编写 `.env.example`**（模板文件，提交Git）