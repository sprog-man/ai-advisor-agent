# 当前开发情况记录

## 2026-06-18 14:45

### Phase 4: 长期记忆系统 — 进行中

#### 已完成
- [x] `memory/vector_store.py` — `WarmVectorMemory` 基于 ChromaDB 的温记忆向量存储
- [x] `memory/knowledge_graph.py` — `WarmKnowledgeGraph` 基于 networkx 的三元组知识图谱
- [x] `memory/cold_store.py` — `ColdLogStore` 基于 SQLite 的原始对话日志冷记忆
- [x] `memory/memory_manager.py` — `MemoryManager` 统一管理温记忆写入/检索和冷记忆记录
- [x] `agents/memory_agent.py` — `MemoryExtractionAgent` 从对话中提炼用户偏好和关键事实
- [x] `state/schemas.py` — 新增 `UserPreference`、`KeyFact`、`MemoryExtraction` Pydantic 模型
- [x] `state/agent_state.py` — 新增 `long_term_context` 和 `extracted_memory` 字段
- [x] `graph/main_graph.py` — 新增 `retrieve_long_term_memory` 和 `record_memory` 节点
- [x] `graph/main_graph.py` — 修复 `should_retry` 条件路由：回退到 `intent` 重新走全流程
- [x] `graph/main_graph.py` — 修复 `conten` → `content` 拼写错误
- [x] `memory/memory_manager.py` — 修复 `format` → `format_results` 方法名和 `["vector",[]]` 切片错误
- [x] `requirements.txt` — 新增 `networkx>=3.0,<4.0` 依赖

#### 已知问题
- [ ] `cold_store.py` 中 `__init__` 方法有大量注释残留，需清理
- [ ] `knowledge_graph.py` 中 `query` 方法有大量注释残留，需清理
- [ ] `should_retry` 挂在 `record_memory` 节点上，但条件判断发生在记录记忆之后，用户说"不满意"时记忆已经被记录了。理想做法是在 `summary` 后、`record_memory` 前做判断
- [ ] `retrieve_long_term_memory` 中 `MemoryManager` 实例是全局单例，多线程环境下需注意线程安全

#### 待开发
- Phase 5: 反思机制 + 多 Agent 协作
- Phase 6: 反馈学习 + 生产保障

---

### Phase 3: 短期记忆 + Checkpoint — 已完成

- [x] `graph/main_graph.py` 引入 `SqliteSaver` 持久化 checkpoint，支持多轮对话
- [x] `main.py` 传入 `config` 激活 checkpoint，`initial_state` 精简为只传 `messages`
- [x] `main.py` 支持 `reset` 命令（更换 thread_id 开始新会话）
- [x] `llm_factory.py` 新增 `_llm_cache` 字典缓存 LLM 实例
- [x] `SummaryAgent` 改用 `self.llm` 缓存实例
- [x] `IntentAgent` 新增 `_compress_history_if_needed` 上下文压缩
- [x] `IntentAgent` 使用 `IntentSchema` 结构化输出

---

### Phase 2: RAG 知识库接入 — 已完成

- [x] `rag/document_loader.py` — 支持 PDF/Word/Markdown 三种格式
- [x] `rag/text_splitter.py` — `SmartTextSplitter` 按 Markdown 标题层级递归分块
- [x] `rag/retrieve.py` — `KnowledgeRetriever` 封装 ChromaDB 向量化存储 + 语义搜索
- [x] `scripts/load_documents.py` — 一键入库脚本
- [x] `data/knowledge_base/` — 存放 3 份 AI 学习文档
- [x] `state/schemas.py` — Pydantic 结构化输出（IntentSchema, PlanSchema, SearchQuery）

---

### Phase 1: 核心骨架 — 已完成

- [x] 配置加载 + LLM 连通
- [x] AgentState + Task 数据结构
- [x] IntentAgent / DecomposeAgent / SummaryAgent
- [x] 主图编排 + 命令行交互入口
